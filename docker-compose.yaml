version: '1.0'

services:

  #nginx:
  #  build:
  #    context: ./.nginx
  #  volumes:
  #    - .:/var/www/appvue
  #  ports:
  #    - 8080:80
  #  networks:
  #    - vueapp

  app:
    container_name: app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8083:80'
    #x-develop:
    #  watch:
    #    - path: ./src/
    #      target: /app/src/
    #      action: sync
